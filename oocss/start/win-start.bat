@echo off

cd ..
vagrant up

@echo =========================================================
@echo vagrant is stopped, you can now close the terminal window
@echo and launch stop.bat for stopping the VM