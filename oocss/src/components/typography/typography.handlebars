<{{elementType}}
{{#if backgroundClassName}}
  {{#if className}}
    class="{{className}} {{backgroundClassName}}"
  {{else}}
    class="{{backgroundClassName}}"
  {{/if}}
{{else}}
  {{#if className}}class="{{className}}"{{/if}}
{{/if}}>{{title}}</{{elementType}}>